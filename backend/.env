# ============================================================================
# NeighborGuard Backend - LOCAL DEVELOPMENT
# ============================================================================

# Server
PORT=3001
NODE_ENV=development

# Database - Railway PostgreSQL (shared for local and production)
# Get this from Railway: Project -> Postgres -> Connect -> DATABASE_URL
DATABASE_URL="postgresql://postgres:QXIPWSxZoNIWuUMEmhrrwLAOjcVZpwzU@metro.proxy.rlwy.net:22058/railway?sslmode=require"

# JWT
JWT_SECRET="neighborguard-dev-secret-key-change-in-production"
JWT_ACCESS_EXPIRES_IN="15m"
JWT_REFRESH_EXPIRES_IN="7d"

# Email (skip email in development, codes shown in console)
DEV_SKIP_EMAIL=true

# Auth - Test Mode
AUTH_CODE_EXPIRES_MINUTES=10
AUTH_CODE_MAX_ATTEMPTS=5
AUTH_TEST_MODE=true
AUTH_TEST_CODE=587585

# Super Admin Email (cannot be deleted)
SUPER_ADMIN_EMAIL=admin@neighborguard.app

# Admin API
ADMIN_SECRET=neighborguard-admin-2024

# Upload
UPLOAD_DIR="./uploads"
MAX_FILE_SIZE=52428800

# Frontend URL (for CORS) - local development
FRONTEND_URL="http://localhost:5173"
